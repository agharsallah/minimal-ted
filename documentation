we have Index father of all Router .. who have these 3 childrens
	
	*App
		+Layout (navbar)
		
	*Projects
		+ Layout (navbar)
		+ Widget (many widget components are called with differnt props (quite static)(most of 		the text props are getted from the Translation file) )
	
	* MapComp (called when user clicks on a readmore link from the widget component which is a dynamic link)
		+Layout (navbar)
		+ChooseMap (where I can choose on link changes than load map X )
			-LeafletMap
			-BlankBallotsDelegation (I will represent blank votes in a google maps choropleth map)
	
	* Municipalities
		+Layout (navbar)
		

	*About
		+Layout (navbar)